server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Dev-Payment-System/payment-server
          search-paths: config-file/**
          default-label: develop

management:
  endpoints:
    web:
      exposure:
        include: refresh